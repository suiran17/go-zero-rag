# 部署前手动执行，不提交到 git：
# kubectl -n rag create secret generic rag-secret \
#   --from-literal=DEEPSEEK_API_KEY=<your-key>
#
# 或 apply 此文件（填入真实值，勿提交到 git）：
apiVersion: v1
kind: Secret
metadata:
  name: rag-secret
  namespace: rag
type: Opaque
stringData:
  DEEPSEEK_API_KEY: "REPLACE_ME"
