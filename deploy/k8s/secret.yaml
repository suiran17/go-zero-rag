# 部署前手动执行，不提交到 git：
# kubectl -n rag create secret generic rag-secret \
#   --from-literal=QWEN_API_KEY=<your-key>
#
# 或 apply 此文件（base64 编码）：
apiVersion: v1
kind: Secret
metadata:
  name: rag-secret
  namespace: rag
type: Opaque
stringData:
  QWEN_API_KEY: "REPLACE_ME"
